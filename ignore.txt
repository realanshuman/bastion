# Bastion ignore list: files that legitimately contain the payload signature
# (documentation, tests, the detector itself). Matches are treated as benign
# REFERENCES, not live threats. One path fragment per line.
# Build configs are only skipped when their exact full path is listed here,
# a fragment like /docs/ never hides the config of a docs site.
SECURITY_INCIDENT
no-config-payload
supply-chain-guard
git-and-security
LOCAL_DEV_AUTH
/docs/
/.security-guard/
.test.
__tests__
.md
